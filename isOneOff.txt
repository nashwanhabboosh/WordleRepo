// checks if all remaining words have only 1 letter not in common
std::vector<char> isOneOff (std::list<std::string> &words) {
    
    // holds the interchangable characters
    std::vector<char> output;

    std::vector<char> tempvec;
    
    // checks whether a mismatching letter has been found in the 
    // current word yet or not; only 1 can be found per word
    bool flag = false;

    // iterate through the letter positions
    for (short i = 0;i<WORD_LENGTH;i++) {

        char letter = '#';

        // iterate through the remaining words
        for (std::string word : words) {
            
            // if its the first word
            if (letter=='#') {
                letter = word[i];
                tempvec.push_back(letter);
            } 
            // if its not the first word, check if the letter in the current
            // position matches that of the previous word
            else if (!flag&&word[i]!=letter) {
                flag = true;
                tempvec.push_back(word[i]);
            } 
            // if not, 
            else if (flag&&word[i]!=letter) {

            } 
            
        }
    }
    
   return output;
}
